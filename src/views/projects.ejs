<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <% projects.forEach(project=> { %>
            <div class="card">
                <h2>
                    <%= project.name %>
                </h2>
                <p>
                    <%= project.description %>
                </p>
            </div>
            <% }) %>
    </main>
    <%- include('partials/footer') %>